{
  "name": "a2z-dispatch-netlify",
  "private": true,
  "version": "1.6.1",
  "scripts": {
    "dev": "netlify dev",
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "pdf-lib": "1.17.1",
    "postgres": "latest"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.3.16",
    "netlify-cli": "latest"
  },
  "engines": {
    "node": ">=22"
  }
}
